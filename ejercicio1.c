#include <stdio.h>

int main () {
        int matriz [3][3] = {
                {4, 1, 8},
                {2, 6, 3},
                {9, 7, 5}
        };
	int elementos = sizeof(matriz) / sizeof(matriz [0][0]);
}
